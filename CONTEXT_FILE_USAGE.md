# 上下文文件使用指南

## 功能介绍

上下文文件功能允许您向AutoShell提供额外的文本文件作为上下文信息，使LLM能够更好地理解您的需求并生成更精准的命令。

## 使用方法

### 基本用法

```bash
# 使用单个上下文文件
python main.py -f examples.txt -c "创建类似的脚本"

# 使用多个上下文文件
python main.py -f config.md -f examples.txt -c "配置环境"
```

### 结合其他模式

```bash
# 自适应模式 + 上下文文件
python main.py --adaptive -f requirements.md -c "检查并安装依赖"

# SSH模式 + 上下文文件
python main.py --ssh-host user@server -f deploy.md -c "部署应用"

# 调试模式 + 上下文文件
python main.py --debug -f config.txt -c "配置系统"
```

## 使用场景

### 1. 提供命令示例

创建 `examples.txt`：
```
# 示例：创建Python虚拟环境
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

使用：
```bash
python main.py -f examples.txt -c "为我的项目创建虚拟环境"
```

### 2. 配置说明

创建 `proxy-config.md`：
```markdown
# 代理配置

公司代理服务器：
- HTTP: http://proxy.company.com:8080
- HTTPS: https://proxy.company.com:8080
- 不使用代理: localhost,127.0.0.1,.company.com

需要设置的环境变量：
- http_proxy
- https_proxy
- no_proxy
```

使用：
```bash
python main.py -f proxy-config.md -c "配置系统代理"
```

### 3. 项目文档

```bash
python main.py -f README.md -f CONTRIBUTING.md -c "创建新的功能模块"
```

### 4. 部署流程

创建 `deploy.md`：
```markdown
# 部署流程

1. 停止旧服务: systemctl stop myapp
2. 备份配置: cp /etc/myapp/config.json /backup/
3. 更新代码: git pull origin main
4. 安装依赖: pip install -r requirements.txt
5. 迁移数据库: python manage.py migrate
6. 启动服务: systemctl start myapp
7. 检查状态: systemctl status myapp
```

使用：
```bash
python main.py --ssh-host user@server -f deploy.md -c "部署最新版本"
```

### 5. 环境配置指令

创建 `env-setup.txt`：
```
# 开发环境配置

安装必要工具：
- git
- python3
- nodejs
- docker

配置Git：
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

配置SSH密钥：
ssh-keygen -t rsa -b 4096
```

使用：
```bash
python main.py -f env-setup.txt -c "配置开发环境"
```

## 支持的文件格式

AutoShell支持以下文本文件格式：

- **文本文件**: `.txt`
- **Markdown**: `.md`
- **配置文件**: `.json`, `.yaml`, `.yml`, `.conf`, `.cfg`, `.ini`, `.toml`
- **脚本文件**: `.sh`, `.bash`, `.py`, `.js`, `.ts`
- **代码文件**: `.c`, `.cpp`, `.java`, `.go`, `.rb`, `.php`
- **其他**: `.xml`, `.html`, `.css`, `.sql`, `.log`, `.env`

## 限制和配置

### 默认限制

- **单个文件最大**: 1MB
- **最多文件数**: 5个
- **支持编码**: UTF-8, GBK, GB2312, Latin-1

### 自定义配置

在 `.env` 文件中配置：

```bash
# 单个文件最大大小（字节）
MAX_CONTEXT_FILE_SIZE=1048576  # 1MB

# 最多文件数量
MAX_CONTEXT_FILES=5

# 默认编码
CONTEXT_FILE_ENCODING=utf-8
```

## 注意事项

### 1. 敏感信息

⚠️ **不要在上下文文件中包含**：
- 密码、API密钥
- 个人敏感信息
- 生产环境凭证
- 私钥文件

✅ **可以包含**：
- 命令示例
- 配置模板
- 项目文档
- 编码规范
- 部署流程

### 2. 文件大小

- 过大的文件可能导致token超限
- 建议每个文件不超过1MB
- 如果文件过大，考虑只提取关键部分

### 3. 文件编码

- 建议使用UTF-8编码
- 系统会自动尝试多种编码
- 如果读取失败，请检查文件编码

### 4. 文件路径

- 使用绝对路径或相对于当前目录的路径
- Windows用户注意路径分隔符（使用 `/` 或 `\\`）

## 实用技巧

### 1. 创建上下文文件模板

为常用任务创建模板文件：

```bash
# 创建模板目录
mkdir -p ~/.autoshell/contexts

# 创建常用模板
echo "# Python项目初始化示例" > ~/.autoshell/contexts/python-init.txt
echo "# Docker部署流程" > ~/.autoshell/contexts/docker-deploy.md
```

使用模板：
```bash
python main.py -f ~/.autoshell/contexts/python-init.txt -c "初始化Python项目"
```

### 2. 组合多个上下文

针对复杂任务，组合多个相关文件：

```bash
python main.py \
  -f project-structure.md \
  -f coding-standards.txt \
  -f deployment-guide.md \
  -c "创建新的微服务模块并部署"
```

### 3. 使用项目文档

直接使用项目的README等文档：

```bash
python main.py -f README.md -c "根据文档说明配置项目"
```

### 4. 临时上下文

为一次性任务创建临时上下文：

```bash
# 创建临时文件
cat > /tmp/temp-context.txt << EOF
需要安装的包：
- nginx
- postgresql
- redis
EOF

# 使用临时文件
python main.py -f /tmp/temp-context.txt -c "安装所需软件"
```

## 常见问题

### Q: 文件读取失败怎么办？

A: 检查以下几点：
1. 文件路径是否正确
2. 文件是否存在
3. 是否有读取权限
4. 文件编码是否正确

### Q: 如何处理大文件？

A: 有几种方法：
1. 只提取文件的关键部分
2. 增加 `MAX_CONTEXT_FILE_SIZE` 配置
3. 分成多个小文件

### Q: 上下文文件会影响性能吗？

A: 
- 文件读取很快，影响很小
- 主要影响是LLM的token使用
- 建议控制文件总大小在几百KB以内

### Q: 可以使用网络文件吗？

A: 当前版本不支持，但可以先下载到本地：
```bash
curl -o /tmp/context.txt https://example.com/context.txt
python main.py -f /tmp/context.txt -c "执行任务"
```

### Q: 如何知道上下文是否生效？

A: 
- 使用 `--debug` 参数查看详细日志
- 观察AI生成的命令是否符合上下文要求
- 检查命令是否使用了上下文中的信息

## 示例场景

### 场景1：配置开发环境

```bash
# 创建环境配置文件
cat > dev-env.md << 'EOF'
# 开发环境要求

## 必需软件
- Python 3.9+
- Node.js 16+
- Docker
- Git

## Python包
- django
- celery
- redis

## Node包
- webpack
- babel
EOF

# 使用上下文配置环境
python main.py -f dev-env.md -c "检查并安装开发环境"
```

### 场景2：数据库迁移

```bash
# 创建迁移指南
cat > migration.txt << 'EOF'
数据库迁移步骤：
1. 备份当前数据库
2. 停止应用服务
3. 执行迁移脚本
4. 验证数据完整性
5. 启动应用服务
EOF

# 执行迁移
python main.py --ssh-host db-server -f migration.txt -c "执行数据库迁移"
```

### 场景3：自动化测试

```bash
# 创建测试要求
cat > test-requirements.md << 'EOF'
# 测试环境要求

## 测试工具
- pytest
- coverage
- selenium

## 测试步骤
1. 运行单元测试
2. 运行集成测试
3. 生成覆盖率报告
4. 检查覆盖率是否达到80%
EOF

# 运行测试
python main.py --adaptive -f test-requirements.md -c "运行完整测试套件"
```

## 总结

上下文文件功能让AutoShell更加灵活和强大：

✅ **优势**：
- 提供精确的上下文信息
- 复用已有文档和配置
- 减少重复输入
- 提高命令准确性

📝 **最佳实践**：
- 创建常用任务的模板文件
- 保持文件简洁明了
- 注意敏感信息安全
- 合理组合多个文件

🚀 **开始使用**：
```bash
# 创建你的第一个上下文文件
echo "# 我的常用命令" > my-commands.txt

# 使用它
python main.py -f my-commands.txt -c "执行类似操作"
```

---

**文档版本**: v1.0  
**更新日期**: 2026-01-22
